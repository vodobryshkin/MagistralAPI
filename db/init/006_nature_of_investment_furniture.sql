-- Миграция для уже существующих баз: добавляем категорию "Мебель" в тип "Характер вложения".
-- Скрипт идемпотентен (IF NOT EXISTS) и безопасен для повторного запуска и для свежих баз,
-- где значение уже присутствует в определении типа.

ALTER TYPE NATURE_OF_INVESTMENT ADD VALUE IF NOT EXISTS 'FURNITURE' BEFORE 'MEDICINES';
